%h1 Listing invoices

%table.table.table-striped
  %tr
    %th Name
    %th Sponsor
    %th Package type
    %th Total
    %th

  - @invoices.each do |invoice|
    %tr
      %td= link_to invoice.name, invoice
      %td= link_to invoice.sponsor.name, invoice.sponsor
      %td= invoice.package_type.name
      %td= invoice.total
      %td= link_to 'Edit', edit_invoice_path(invoice)

%br

%a.btn.btn-primary{href: new_invoice_path} New Invoice
