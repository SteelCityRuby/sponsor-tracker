%h1 Listing invoices

%table.table.table-striped
  %tr
    %th Sponsor
    %th Package type
    %th Total
    %th
    %th

  - @invoices.each do |invoice|
    %tr
      %td= invoice.sponsor.name
      %td= invoice.package_type.name
      %td= invoice.total
      %td= link_to 'Show', invoice
      %td= link_to 'Edit', edit_invoice_path(invoice)

%br

%a.btn.btn-primary{href: new_invoice_path} New Invoice
