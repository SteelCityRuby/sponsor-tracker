= page_header 'Listing package_addon_types'

%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Price
      %th Quantity remaining
      %th Total spots

  %tbody
    - @package_addon_types.each do |package_addon_type|
      %tr
        %td= link_to package_addon_type.name, package_addon_type
        %td= number_to_currency package_addon_type.price
        %td= package_addon_type.remaining
        %td= package_addon_type.total_spots

%br

%a.btn.btn-primary{href: new_package_addon_type_path}
  %span.glyphicon.glyphicon-plus
  New Package addon type
